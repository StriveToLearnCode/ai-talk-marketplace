interface:
  display_name: "AI Talk"
  short_description: "连续保存任务状态，按风险渐进升级，严格守住写入边界"
  default_prompt: "使用 $ai-talk 连续处理这条研发任务；简单任务保持轻量，实际介入时说明可核验的判断、依据和影响。"

policy:
  allow_implicit_invocation: true
