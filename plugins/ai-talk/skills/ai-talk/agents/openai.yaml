interface:
  display_name: "AI Talk"
  short_description: "澄清关键需求并对 UI 异常执行浏览器只读运行态取证"
  default_prompt: "使用 $ai-talk 澄清这项开发需求；若是 UI 异常，直接检查浏览器运行态证据并给出只读诊断结论。"

policy:
  allow_implicit_invocation: false
