interface:
  display_name: "AI Talk"
  short_description: "只生成优化提示词，不读取或执行下游流程"
  default_prompt: "使用 $ai-talk 把我的研发需求整理成可复制执行的提示词；可以在 text 代码块中指定项目 Skill，但当前轮不要读取业务文件、调用 Skill 或执行任务。"

policy:
  allow_implicit_invocation: false
