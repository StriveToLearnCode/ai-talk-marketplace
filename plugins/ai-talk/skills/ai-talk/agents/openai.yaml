interface:
  display_name: "AI Talk"
  short_description: "发现并复用已有能力，整理需求后让 Codex 同线程继续"
  default_prompt: "使用 $ai-talk 发现可复用的 Skill、组件和规范，整理我的开发需求并在确认后继续处理。"

policy:
  allow_implicit_invocation: false
