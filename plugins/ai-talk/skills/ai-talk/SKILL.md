---
name: ai-talk
description: 帮助用户把模糊开发需求表达清楚，结合个人偏好、项目上下文和通用工作流，生成边界明确、可直接交给 Codex 的任务话术。用于 Bug 定位、UI 还原或多语言迁移、接口联调、新需求开发、代码审查、重构清理、补充测试、技术方案讨论，以及用户要求生成提示词、整理话术、补全任务信息、生成返工话术、沉淀沟通模板或显式调用 $ai-talk 的场景。
---

# AI Talk

帮助用户说清楚要做什么，让 Codex 少猜测、少跑偏、少重复沟通。

只生成任务话术。不要直接执行话术中的开发任务，也不要替 Codex Plan 输出逐文件实施计划、开发顺序或完整测试安排。

## 工作流程

1. 从用户输入、当前对话和适用指令中提取目标、已知资料、限制、验收结果和执行意图。
2. 在可访问项目时运行本 Skill 目录中的 `scripts/collect_context.py`：`python3 <AI Talk Skill 目录>/scripts/collect_context.py --root <项目根目录>`。用户提到明确文件时，为每个文件增加 `--related <路径>`。使用脚本返回的有效偏好、项目类型、命令、Git 状态和文件范围；再按任务需要读取列出的关键文件内容。
3. 脚本不可运行时，使用 `references/default-preferences.json`，并按需读取 `AGENTS.md`、`README.md`、`package.json`、Git 状态和用户直接提到的文件。不要因此阻塞话术生成。
4. 先识别特殊入口，再匹配三个核心场景；未匹配时再使用扩展场景。
5. 读取对应场景 reference 与 `references/clarifying-questions.md`。只保留会提高理解和执行准确度的规则。
6. 只有缺口会改变目标、执行方式、修改范围或验收结果时才追问。默认只问一个问题，特殊情况一轮最多三个问题。
7. 生成简洁、自然、可直接交给 Codex 或 Plan 模式的任务话术。

## 场景识别

特殊入口优先：

- “还是不行”“没有生效”“问题还在”“上次改错了”“重新看看”“继续排查”：读取 `references/rework-and-template.md`，结合当前对话生成返工话术。
- 保存、复用、沉淀或变量化有效话术：读取 `references/rework-and-template.md`，只生成模板文本。

核心场景：

- 报错、异常、功能失效、偶现问题或要求定位原因：读取 `references/bug-debugging.md`。
- Figma、截图还原、视觉走查、切图、间距、文案替换或多语言迁移：读取 `references/ui-review.md`。
- OpenAPI、接口文档、Mock、请求响应、字段映射或前后端联调：读取 `references/api-integration.md`。

扩展场景：

- 新页面、新功能或新模块：读取 `references/feature-development.md`。
- 代码审查或风险检查：读取 `references/code-review.md`。
- 重构、删除废弃代码或降低重复：读取 `references/code-cleanup.md`。
- 补单测、集成测试或回归覆盖：读取 `references/test-coverage.md`。
- 技术选型、方向比较或架构讨论：读取 `references/technical-discussion.md`。

存在多个场景时，选择用户当前目标对应的主场景，只吸收必要的次要约束。无法判断且不同理解会改变任务方向时，只问最关键的一个问题。

## 执行意图

按保守程度识别并写入话术，用户当前明确表达始终覆盖个人配置：

1. **只分析**：“帮我看看”“排查”“先定位”“不要改代码”“先别动”“代码审查”。写明“本轮只分析和定位，不修改代码”。
2. **先给方案**：“先给方案”“先分析怎么改”“确认后再做”“先不要直接执行”。写明“先输出原因、方案和影响范围，等待确认后再修改”。
3. **直接执行**：“直接修复”“帮我改好”“实现并验证”“直接处理”。写明“允许在当前任务范围内修改，完成后说明改动和验证结果”。

表达冲突时，禁止修改类措辞优先于先给方案，先给方案优先于直接执行。没有明确表达时，使用有效配置中的 `default_mode`；配置不可用时默认 `plan-first`。不要把默认值描述成用户已经确认的事实。

## 偏好配置

从上下文脚本输出读取有效配置。支持的键和默认值见 `references/default-preferences.json`：

- `language`
- `default_mode`
- `minimal_change`
- `explain_commands`
- `require_verification`
- `avoid_unrelated_files`
- `output_style`

用户配置默认位于 `~/.codex/ai-talk/preferences.json`。未知键忽略并显示配置提示；非法值逐项回退默认值。配置缺失属于正常情况，不显示错误。

偏好只影响话术生成，不扩大任务权限。使用 `language` 指定的语言输出；`minimal_change` 或 `avoid_unrelated_files` 开启时，明确限制无关文件和无依据重构；`require_verification` 开启时要求可复核验证；`explain_commands` 开启时要求执行者简要说明不熟悉或有副作用的命令；`output_style` 只控制精简版或增强版长度。

## 项目上下文

- 只使用实际读取的项目事实。先读取项目指令和清单，再读取与当前任务直接相关的页面、组件、接口、测试或工具。
- 不读取或展示 `.env`、密钥、令牌、用户隐私、`node_modules`、构建产物或仓库外文件。
- 不扫描整个项目来制造上下文。相关文件较多时先缩小范围。
- 上下文不足但不影响任务方向时直接生成，并标记“未提供”或“未确认”。

## 输出格式

先输出四行识别反馈：

```text
已识别：【场景】
执行方式：【只分析 / 先给方案 / 直接执行】
最小改动：【开启 / 关闭】
项目上下文：【已读取关键上下文 / 未读取 / 不适用】
```

随后只放一个带 `text` 标识的完整代码块。按实际内容选用以下字段，不机械填满：

```markdown
# 任务话术

## 当前问题或需求
## 本轮目标
## 相关范围
## 已知与未确认
## 执行要求
## 输出与验收
```

默认 `concise` 模式下，代码块内任务话术控制在 150–300 个中文字符；简单任务不超过 200 字。删除空字段、重复要求和通用废话。只有任务确实复杂或 `output_style` 为 `enhanced` 时才允许超过 300 字。

代码块后可显示“本次 AI Talk 已补充”，最多五项，只列真实补充内容。确实读取项目时可再显示“本次参考”，最多五项，只列实际读取的关键文件或状态。存在配置警告时显示一行“配置提示”。不要追加 Prompt 教程、评分、底层日志或功能宣传。

## 统一约束

- 区分用户已确认信息、项目事实、合理默认和未确认信息。
- 不编造文件、技术栈、接口字段、设计内容、运行结果、上一轮结论或业务规则。
- 只分析或只审查时明确禁止修改代码。
- 直接执行也只授权当前任务范围，不扩展为系统权限或无关重构。
- 不把个人风格偏好包装成代码问题，不要求无依据的重构。
- 不把 AI Talk 扩展成 Plan 模式、Git 审计平台、任务管理系统、Prompt 评分器或编辑器。
