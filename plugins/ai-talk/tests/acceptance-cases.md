# AI Talk v1 新线程验收

每个用例都在重新安装插件后的全新 Codex 对话中运行。只计算 `text` 代码块内的任务话术字符数；识别反馈、补充项和参考文件不计入 300 字限制。测试结果栏保持空白，实际执行后再填写。

## 1. Bug 定位，只分析

输入：

```text
$ai-talk 轮播图偶尔不切换，先帮我看看
```

通过条件：

- 场景为“Bug 定位”，执行方式为“只分析”。
- 明确禁止修改代码，范围包含生命周期、状态、定时器或异步时序。
- 要求用代码位置、日志、请求、断点或最小实验提供证据，并给出验证方式。
- 不虚构复现步骤、文件路径或根因；任务话术为 150–300 字。

结果：未执行

## 2. UI 与多语言遗漏，只分析

输入：

```text
$ai-talk 这个活动原来是俄语，现在改成法语，看看还有哪些没替换
```

通过条件：

- 场景为“UI/多语言迁移”，执行方式为“只分析”。
- 检查代码文字、语言资源、图片文案和接口动态内容。
- 要求截图或页面对比，不修改业务逻辑，不增加对照资料中不存在的内容。
- 不声称已读取未提供的设计稿或页面；任务话术为 150–300 字。

结果：未执行

## 3. 接口联调，默认先给方案

输入：

```text
$ai-talk 根据这个接口文档把奖励接口接一下
```

通过条件：

- 场景为“接口联调”，执行方式标明采用默认“先给方案”。
- 以接口文档或真实响应为准，不编造字段。
- 覆盖请求参数、响应映射、加载、空数据、失败和重复请求。
- 不授权修改无关公共请求代码；任务话术为 150–300 字。

结果：未执行

## 4. 接口联调，直接执行

输入：

```text
$ai-talk 根据接口文档直接把奖励接口接好并验证
```

通过条件：

- 场景为“接口联调”，执行方式为“直接执行”。
- 只允许修改当前模块及直接请求封装，完成后说明改动和验证结果。
- 仍然要求契约依据、异常状态和真实请求响应验证，不能因直接执行而放宽边界。
- 任务话术为 150–300 字。

结果：未执行

## 5. “还是不行”返工

先在同一对话提供上一轮信息：轮播图被判断为页面隐藏后定时器未恢复，修改了轮播组件并通过单测，但测试环境仍偶尔不切换。随后输入：

```text
$ai-talk 还是不行
```

通过条件：

- 场景为“返工”，默认先重新定位，再决定是否修改。
- 复用已提供的上一轮判断、修改和验证，不要求重新描述全部背景。
- 检查修改是否生效、环境是否使用最新代码，以及构建、缓存、部署或启动进程。
- 不重复无证据结论，不虚构新根因；任务话术为 150–300 字。

结果：未执行
