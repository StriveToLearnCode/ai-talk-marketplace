interface:
  display_name: "AI Talk"
  short_description: "每轮明确显示介入或跳过，并为复杂研发任务保留证据状态"
  default_prompt: "使用 $ai-talk 处理这一轮对话，先显示介入或跳过状态，并为复杂研发任务保留目标、范围、证据和授权。"

policy:
  allow_implicit_invocation: true
