---
name: ai-talk
description: Use when 发起或继续任何软件研发工作，包括单轮修改、排查、评审和修改 AI Talk；每次使用都说明这一次具体做了什么，仅在纠正、边界或交接时持久化。非研发、状态/位置询问、偏好反馈、无修改请求的元讨论和只有引用时不触发。
---

# AI Talk 下一步状态

每次研发请求先加载本 Skill 一次；“是否加载”和“是否持久化”是两个独立决定。当前消息即使很短，只要已有对话明确它是在继续研发任务，例如“继续”“执行”或“完成1I”，仍需加载。

只维护会改变下一步实现、验证、范围或授权的信息，让任务 Agent 知道：现在只做什么、什么已经正确不能回归、用户纠正了什么、允许做什么，以及怎样才算完成。

不要保存原始日志、搜索或思维过程、文件正文、完整响应、截图内容、secret、token 或 cookie。不要代替任务 Agent 规划、诊断、编码、发布或验证。

## 核心流程

1. 从当前消息和已有对话绑定一个目标，并提炼：
   - `goal`：一个可观察的产品结果，不写实现步骤。
   - `boundaries`：明确允许和禁止的对象或动作；本地修改、commit、push、外部保存和发布不能互相推导，外部写入默认未授权。
   - `acceptance`：能在指定环境观察的完成条件，不用“代码已修改”代替。
   - `bindings`：下一步需要复用且来源明确的 URL、报错摘要、目标文件、组件、业务 ID、配置 key 或截图位置。
2. 用户已给出的内容直接采用，不要求按模板重述。只有缺失或冲突会改变产品结果、修改范围或授权时，才问一个决定性问题；否则让任务 Agent 直接执行并按风险验证。
3. 加载后判断是否需要持久化。明确、局部、单轮可完成且没有跨轮保护项或边界时，不建立状态；仍需说明 AI Talk 本轮具体明确了什么，不得把“不持久化”或“不输出状态”误判为没有可见贡献。
4. 只有出现下列任一情况时才建立或更新状态：
   - 用户纠正了会改变实现方式的规则。
   - 已确认结果后续可能被改坏。
   - 范围、并行改动或外部写入有明确边界。
   - 任务需要跨上下文、压缩或交接。
   - 验收已部分完成，或稳定标识需要在下一轮复用。
5. 一旦需要持久化、恢复或修改已有状态，先完整读取 [状态协议](references/task-state-protocol.md)，再使用其中的 schema、生命周期和脚本命令。脚本路径必须相对本 Skill 目录解析，不能假设当前工作目录就是 Skill 目录。

页面 URL、报错或组件名只用于定位，不自动证明根因、扩大范围或授予外部写权限。

## 接受纠正

用户说“停，保留 A，撤掉 B”时，立即停止未执行动作并更新下一步：

- A 是用户确认正确的可观察结果时，保护为 `confirmed_results`；A 只是不能触碰的对象或改动时，记为禁止 `boundaries`。
- B 成为最新 `corrections` 和唯一 `next_action`，替换冲突旧方案。
- 撤销前重读当前 diff 和目标文件，只撤掉能归属于 B 的改动；保留 A、用户原有改动和并行改动，禁止整文件覆盖、全量 reset 或无差别回滚。
- A、B 无法唯一分离或撤销 B 必然破坏 A 时，只问一个决定性问题。

## 修改前保护

已有持久状态时，每次首次修改目标代码前，以及恢复上下文后的首次修改前，都运行状态协议中的 `preflight`。先把输出的不可回归项和禁止边界作为编辑约束；目标文件指纹变化时，将修改者视为未知，重读现状并合并，不能覆盖变化。

没有持久状态的局部任务不为执行 `preflight` 创建空状态，也不虚构保护项。

## 对外表达

每一次实际使用 AI Talk，都必须用一句自然语言说明这一次具体明确、补充或保护了什么，让用户能直接感知它的作用。固定顺序是：先说明将使用 AI Talk，再完成本 Skill 的读取、分析和必要的状态操作，随后在紧接着的下一段输出 `AI Talk 分析结果：<本次具体作用>`，然后才能开始业务目录定位、源码检索、实现或验证；中间不得插入任务 Agent 的进度说明或工具调用。

分析结果至少包含目标、边界、验收、绑定或跨轮保护中的一项当前任务具体内容。不能只说“会按 AI Talk 执行”或“已读取 Skill”，也不能用与当前任务无关的通用套话代替具体贡献。最终总结不能承担本轮首次 AI Talk 贡献说明；结尾只按需回顾，不重复冒充即时结果。

只报告真正影响本轮下一步的内容，不展示内部 YAML、协议名、路由或工作回放，不冒领任务 Agent 的搜索、编码、测试或发布。没有持久化时也可以直接说明目标和验收，但不能声称“已记录”或“已保护”。

当 AI Talk 因用户追加或纠正而补充、改写目标时，必须写成 `AI Talk 目标补充为：<当前唯一目标>`。同一轮不重复播报；后续轮次只说明本轮仍会实际约束下一步的具体内容。

交接或结束时按需说明：不可回归项、当前目标、最新纠正、禁止边界、验收、唯一下一步和未授权动作；没有的部分省略。

用户最新明确要求高于旧实现、旧交接和持久状态。普通反馈、纯 AI Talk 元讨论、状态或位置询问，以及只有引用而没有当前研发请求时保持静默；用户明确要求修改 AI Talk 自身时属于研发任务。
