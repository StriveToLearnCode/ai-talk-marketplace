---
name: ai-talk
description: Use when 发起或继续软件研发工作，包括修改、排查、评审和修改 AI Talk；把当前请求收敛为轻量任务状态，绑定仓库位置，保存源码或运行验证过的事实，并在修改前保护不可回归项。非研发、状态或位置询问、偏好反馈、无修改请求的元讨论和只有引用时不触发。
---

# AI Talk 任务状态

每次研发请求加载一次。AI Talk 只回答三件事：当前做什么、哪些事实已经证实且不能被改坏、怎样才算完成。就近 `AGENTS.md` 规定代码怎么写，业务 Skill 决定复用什么组件、API 或玩法，AI Talk 不重复这些规则。

## 工作流

1. 从用户最新要求提取一个可观察的 `current_goal`，同时明确 `change_boundaries`、`completion_criteria` 和唯一 `next_action`。本地修改不推导出 commit、push、发布或外部写入权限。
2. 在活动仓库运行 `scripts/collect-task-context.mjs --root <当前工作区> [--target <目标文件> ...]`。采用脚本核实的当前分支、活动目录、页面目录和最近的 `AGENTS.md`；分支定位与用户明确路径冲突时只问一个决定性问题。读取最近的 `AGENTS.md`，不要把其编码规则复制进任务状态。
3. 只把经过源码或运行验证的信息加入 `verified_facts`：实际负责目标区域的组件、已核实的接口或配置 key、通过的测试，以及用户明确要求不可回归的结果。每项带简短来源和验证方式；文件名猜测、模型记忆、未运行的测试和单独截图不算已验证事实。
4. 把尚未得到证据的验收动作放入 `pending_checks`。每完成一步，立即删除已完成检查，按结果补充 `verified_facts`，并把 `next_action` 替换为新的唯一下一步。代码写完但仍有 `pending_checks` 时不得宣告完成。
5. 修改前先检查所有 `protected: true` 的事实和禁止边界；已有持久状态时运行 `preflight`。目标文件、分支或最近的 `AGENTS.md` 变化后，重读现状并基于当前内容合并，不能覆盖来源未知的变化。

页面 URL、截图、报错或组件名只用于定位，不证明根因，不扩大修改范围或授权。需要定位 UI 时，沿 `路由 -> 页面入口 -> import/注册 -> 最小负责组件 -> 数据或事件链` 核实；只保存最终得到源码证据的结论。

## 状态闭环

任务状态固定维护：

- `current_goal`：唯一产品结果。
- `change_boundaries`：允许、禁止或实现约束。
- `verified_facts`：有来源的源码、运行或用户保护事实。
- `pending_checks`：仍需执行的验证。
- `completion_criteria`：可观察的完成条件。
- `next_action`：唯一可执行下一步。

明确、局部、单轮即可完成且不需要跨轮保护时，只在当前上下文维护，不创建状态文件。出现用户纠正、不可回归项、范围变化、部分验收、上下文压缩或交接时，先完整读取 [状态协议](references/task-state-protocol.md)，再用其中脚本保存或恢复；不要只在对话中声称已记录。

## 纠正与保护

用户说“保留 A，撤掉 B”时，把 A 中可观察且不能回归的结果记为受保护事实，把 B 变成当前边界或唯一下一步。撤销前重读 diff，只撤销能明确归属于 B 的改动；无法与 A、用户原有改动或并行改动分离时再询问。

用户最新明确要求高于旧状态。事实的成立条件失效时删除或降回待验证，不能因为它曾经正确就永久复用。

## 对外表达

加载并分析后，在开始源码检索或实现前只输出一句：

> AI Talk 分析结果：目标是修复奖励弹窗重复请求，仅修改 reward-dialog 调用链，以接口只触发一次且弹窗正常展示为验收。

句子按需包含目标、边界和验收，不逐字段播报。只有发生用户纠正、范围变化或跨轮交接时，才补充不可回归项、待验证项和唯一下一步。最终总结只报告真实验证结果与剩余项。
