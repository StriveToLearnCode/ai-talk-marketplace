# AI Talk Formatter 前后对比

以下案例来自仓库现有截图 fixture、路由验收输入和真实 CLI 输出。修改前输出取自本次改动前的 Formatter，修改后输出取自重构后的同一条路由链。

## 案例 1：截图开发积分奖励 UI

输入：`帮我开发这个图，3 是目标 UI。`

修改前：981 字，依次展开开发目标、截图理解、UI 结构、交互、数据需求、可复用资源、待定位、约束、验收目标、执行授权和建议 Skill。目标 UI 的可见内容、附件来源和多个 search-resolvable unknown 占据了主体。

修改后：257 字，为修改前的 26.2%。

```text
🎯 任务目标
实现图3对应的积分奖励阶段 UI。

🧠 AI 推断
这是奖励页面的积分阶段扩展；应沿现有奖励展示、状态判断和页面跳转链路接入，避免建立平行实现。

📚 需要理解
- 积分阶段
- 奖励展示
- 奖励状态
- 页面交互

🔍 优先检索
📈 积分阶段
→ Page Center Banner（复用与积分阶段相关的已有资源）
🎁 奖励展示
→ Page Center Playground（复用与奖励展示相关的已有资源）

▶ 下一步
当前阶段：修改代码
建议 Skill：gen-code
```

变化：不再复述三张附件、五个 UI 元素、三个数据 unknown 和四条验收项；这些事实仍保留在 `execution_plan`。

## 案例 2：奖励名称和角标缺失

输入：`修复奖励名称和角标缺失的问题`

修改前：只给出原句式开发目标、待定位、约束、执行授权和 Skill，真正的数据链判断与入口没有进入 Formatter。

修改后：

```text
🎯 任务目标
修复奖励名称和角标缺失的问题。

🧠 AI 推断
奖励名称和角标同时缺失，问题跨越抽奖接口、数据适配和弹窗渲染三层，应沿现有数据链定位，不先假定字段含义。

📚 需要理解
- 奖励名称和角标的接口字段
- 抽奖结果到弹窗数据的适配
- 奖励弹窗的字段渲染

🔍 优先检索
🎁 奖励名称和角标的接口字段
→ do_lottery（确认奖励名称和角标对应的接口字段）
🔄 抽奖结果到弹窗数据的适配
→ openRewardDialog（确认抽奖结果到奖励弹窗数据的适配方式）

⚠️ 待确认
- 缺少 gen-code Skill，需先安装或启用

▶ 下一步
当前阶段：修改代码
建议 Skill：gen-code（需安装或启用）
```

变化：从“列边界”改为“判断问题跨三层，并指出先查哪两个真实实现”。

## 案例 3：claimed 图片不切换

输入：`定位 claimed 状态下图片不切换的代码原因，只分析，不修改代码`

修改前：重复完整输入，另列 `state | claimed | 用户`、非阻塞待定位、两条约束和执行授权。

修改后：

```text
🎯 任务目标
修复 claimed 状态下的图片切换异常。

🧠 AI 推断
这是状态到图片渲染链路的适配异常；应依次核对状态来源、状态转换和图片渲染分支。

📚 需要理解
- 状态来源
- 状态转换
- 图片渲染分支

🔍 优先检索
🎨 状态来源
→ claimed（从该状态标识追踪数据来源）
🎨 状态转换
→ isClaimed（确认接口状态到视图状态的转换逻辑）

⚠️ 待确认
- 缺少 gen-code Skill，需先安装或启用

▶ 下一步
当前阶段：定位问题
建议 Skill：gen-code（需安装或启用）
```

变化：`claimed` 不再作为孤立事实展示，而是成为“状态来源 → 状态转换 → 图片分支”的定位链。

## 案例 4：Midscene 自动化测试

输入：`为 recharge 页面生成 Midscene 自动化测试文件`

修改前：重复输入，列出 `target_page | recharge | 用户`、一条通用约束、执行授权和缺失 Skill。

修改后：

```text
🎯 任务目标
新增 recharge 页面的自动化测试。

🧠 AI 推断
这是现有页面流程的自动化覆盖；应先确认关键路径、稳定断言和项目测试约定，再生成可运行用例。

📚 需要理解
- 目标流程
- 关键断言
- 测试运行约定

🔍 优先检索
• 目标流程
→ recharge（定位目标流程的现有实现）

⚠️ 待确认
- 缺少 ai-test Skill，需先安装或启用

▶ 下一步
当前阶段：自动化测试
建议 Skill：ai-test（需安装或启用）
```

变化：从“页面事实 + 通用约束”改为测试执行前真正要理解的流程、断言和运行约定。

## 案例 5：页面现场检查

输入：`打开 recharge 页面检查视觉、交互和响应式，只检查不修改代码`

修改前：重复输入，列出目标页面、只读约束、执行授权和 Skill。

修改后：

```text
🎯 任务目标
完成 recharge 页面的页面检查。

🧠 AI 推断
这是页面现场检查任务；应从目标行为、交互状态和响应式表现定位可复现问题。

📚 需要理解
- 目标页面行为
- 交互状态
- 响应式表现

🔍 优先检索
• 目标页面行为
→ recharge（定位目标页面行为的现有实现）

▶ 下一步
当前阶段：页面检查
建议 Skill：ui-self-check
```

变化：无硬阻塞，因此隐藏待确认；执行边界保留在 JSON 协议，不再抢占用户可见层级。
