import assert from "node:assert/strict";import{execFile}from"node:child_process";import{mkdir,mkdtemp,rm,writeFile}from"node:fs/promises";import os from"node:os";import path from"node:path";import{promisify}from"node:util";import test from"node:test";const run=promisify(execFile),script=path.resolve(import.meta.dirname,"../scripts/build-capability-index.mjs");test("compatibility entrypoint delegates to MVP router",async t=>{const root=await mkdtemp(path.join(os.tmpdir(),"ai-talk-entry-")),d=path.join(root,".agents","skills","fe-gen-code");t.after(()=>rm(root,{recursive:true,force:true}));await mkdir(d,{recursive:true});await writeFile(path.join(d,"SKILL.md"),"---\nname: gen-code\ndescription: 生成页面代码和业务逻辑。\n---\n");const p=JSON.parse((await run(process.execPath,[script,"--root",root,"--query","把这个页面做出来并修改代码"],{encoding:"utf8"})).stdout);assert.equal(p.recommendation.name,"gen-code");});
