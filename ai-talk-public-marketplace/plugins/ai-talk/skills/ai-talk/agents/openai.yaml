interface:
  display_name: "AI Talk"
  short_description: "有界读取明确文件，只生成优化提示词"
  default_prompt: "使用 $ai-talk:ai-talk 把我的研发需求整理成可复制执行的提示词；可读取我明确附带或指定的最多 3 个本地文件，每个最多 64KB，但不要扫描项目、读取依赖、调用 Skill 或执行任务。"

policy:
  allow_implicit_invocation: false
