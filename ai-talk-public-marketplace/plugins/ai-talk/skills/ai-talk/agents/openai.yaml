interface:
  display_name: "AI Talk"
  short_description: "用短摘要理解研发需求，并折叠生成可复制的完整执行提示词"
  default_prompt: "使用 $ai-talk:ai-talk 结合我附带的截图和需求，先用目标、事实和执行时自动补充给出短摘要，再将精简且可复制的完整 Prompt 默认折叠；不要执行任务。"

policy:
  allow_implicit_invocation: false
