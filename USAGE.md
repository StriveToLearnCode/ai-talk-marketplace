# AI Talk 使用说明

## 调用

```text
$ai-talk <研发任务>
```

AI Talk 生成简短执行协议后停止。它只在用户明确指出的目标范围内读取上下文，不修改文件，也不自动调用建议 Skill。

## 示例

输入：

```text
$ai-talk 在 banner-spin.vue 中，积分阶段 PROGRESS_TASK_ID: 7，然后一样展示进度和奖励
```

输出：

```text
用户目标：
积分阶段接入任务 7，复用现有方式展示进度和奖励。

项目上下文：
- 目标文件：banner-spin.vue

建议检索：
- 积分阶段任务关联
- 进度展示逻辑
- 奖励展示逻辑

实现边界：
- 修改范围限于 banner-spin.vue 及必要直接依赖
- 不引入用户未确认的业务逻辑

建议 Skill：
gen-code
```

这个输入没有足够证据形成有价值的任务定位，因此省略“任务定位”。目标文件不存在、不可读、越界或超过 128 KiB 时，AI Talk 不会扩大扫描范围。

## 定位与边界

- 任务定位只说明任务类型、实现对象和检索重点，保持 1～2 句且不超过 100 个中文字符。
- 不输出具体代码方案、推荐 API、猜测字段或未经确认的参数处理方式。
- 建议检索最多 5 条，必须是可直接搜索的高命中短语。
- 实现边界最多 2 条，只保留用户明确要求或可靠项目边界。

用户明确要求与默认规则冲突时，以用户目标为准并删除冲突规则；真实代码事实不得升级成实现结论。

## 项目读取边界

- 只读取显式目标、沿路径 `AGENTS.md`、一层相对本地依赖和真实附件。
- 不读取 `node_modules`、仓库外符号链接、无关兄弟模块或递归目录。
- 最多读取 8 个文件，单文件最多 128 KiB。

`--debug-json` 输出 schema v6，并保留旧路由字段以兼容现有评估工具。
